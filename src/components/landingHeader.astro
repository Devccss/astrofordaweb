---
---

<div id="header" class="bg-black fixed h-16 w-full rounded-sm ">
    <section class="flex h-full justify-between items-center px-4 font-semibold">
      <nav>
        <a href="/" data-astro-prefetch class="text-3xl items-center">
          4DAWEB
        </a>
      </nav>
      <div class="text-center ">
          <p class=" p-2 items-center text-white in-hover:text-transparent text-2xl select-none">No olvides contactarme !</p>
      </div>
      <nav class="text-center">
        <a href="/contact" data-astro-prefetch class=" p-2 items-center">
          Contactanos
        </a>
      </nav>
    
    </section>
</div>
<!-- <script>
  let timeout: ReturnType<typeof setTimeout>;
  window.addEventListener("scroll", () => {
    const header = document.getElementById("header");

    // Muestra el header cuando el usuario está interactuando
    if (header) {
      header.classList.remove("-translate-y-full");
    }

    // Reinicia el temporizador cada vez que el usuario se mueve
    clearTimeout(timeout);
    timeout = setTimeout(() => {
      if (header) {
        header.classList.add("-translate-y-full");
      }
    }, 2000); // Se oculta después de 2 segundos sin movimiento
  });
</script> -->